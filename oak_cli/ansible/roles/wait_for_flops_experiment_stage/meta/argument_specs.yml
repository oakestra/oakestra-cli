argument_specs:
  main:
    options:
      stage_indicator:
        type: str
        required: true
        description: "Specific log keyword(s) that indicate that a certain stage has been reach (FLOps Project, e.g. The Builder is ready)"
