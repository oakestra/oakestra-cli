## oak application create

Create one or multiple applications from an SLA file

### Synopsis

Create applications defined in an SLA JSON file.

SLA file resolution order:
  1. The --file / -f flag (absolute or relative to current directory)
  2. A positional argument — searched in the current directory, then in ~/oak_cli/SLAs/
  3. Interactive picker from ~/oak_cli/SLAs/

Example:
  oak app create                         # interactive picker
  oak app create myapp                   # resolves myapp.json
  oak app create -f ./custom.json        # explicit path
  oak app create edge_gaming -d          # create + deploy all services

```
oak application create [sla-name] [flags]
```

### Options

```
  -d, --deploy        Deploy all services after creating the application
  -f, --file string   Path to the SLA JSON file (optional; opens interactive picker if omitted)
  -h, --help          help for create
```

### SEE ALSO

* [oak application](oak_application.md)	 - Manage Oakestra applications

###### Auto generated by spf13/cobra on 30-Mar-2026
