## oak install worker

Install a NodeEngine worker node

### Synopsis

Install a NodeEngine worker node on this machine.

Prerequisites:
  - At least one cluster orchestrator must be running and registered
    with the root orchestrator.

After installation the CLI will let you pick which cluster this
worker should join and will configure NodeEngine automatically.

```
oak install worker [version] [flags]
```

### Options

```
  -h, --help   help for worker
  -y, --yes    Skip confirmation prompt
```

### Options inherited from parent commands

```
      --root   Run install scripts with sudo
```

### SEE ALSO

* [oak install](oak_install.md)	 - Install Oakestra components on this machine

###### Auto generated by spf13/cobra on 30-Mar-2026
